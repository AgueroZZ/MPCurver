# Publish MPCurver 0.3.1

**Goal:** Publish effective-ordering diagnostics and uniform-prior greedy dimension selection as MPCurver 0.3.1 on GitHub and the curated package website.

## Release steps

- [x] Set `DESCRIPTION` to 0.3.1 and include `select_mpcurve_dimension` in the public reference allowlist.
- [x] Regenerate documentation; run the test suite and package check.
- [x] Rebuild `docs/` with `scripts/build_public_site.R` and audit the public pages, links, and internal-content exclusions.
- [x] Stage only package changes, curated website output, and this release's plan/log; inspect the staged file list and diff.
- [x] Commit and push `main`, then verify the remote package version and deployed website.

Local experiments and unrelated untracked records remain outside the release commit.
