% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11_dimension_selection.R
\name{select_mpcurve_dimension}
\alias{select_mpcurve_dimension}
\title{Select the number of intrinsic orderings under a uniform prior}
\usage{
select_mpcurve_dimension(
  X,
  max_intrinsic_dim,
  direction = c("forward", "backward"),
  ...
)
}
\arguments{
\item{X}{Sample-by-feature data matrix, as for \code{\link[=fit_mpcurve]{fit_mpcurve()}}.}

\item{max_intrinsic_dim}{Positive integer upper bound for the search.}

\item{direction}{Either \code{"forward"} or \code{"backward"}.}

\item{...}{Additional fixed-model arguments passed to \code{\link[=fit_mpcurve]{fit_mpcurve()}}.
The selector controls \code{intrinsic_dim}, \code{greedy}, \code{partition_init},
\code{partition_prior}, \code{partition_prior_init}, and \code{hard_assign_final}.
A scalar \code{method} may be supplied. Dimension-specific initial fits are
not accepted through \code{...}.}
}
\value{
The selected \code{\link[=fit_mpcurve]{fit_mpcurve()}} \code{mpcurve} object. Its
\verb{$dimension_selection} record contains \code{direction},
\code{max_intrinsic_dim}, \code{selected_M}, \code{stop_reason}, a
\code{continued_after_selection} flag, a \verb{$candidates} table
with every attempted fit and its score, convergence state, warnings, and
error, and a \verb{$history} table including the rejected stopping step.
}
\description{
Fit adjacent candidate dimensions with a fixed uniform prior over feature
orderings. Forward selection starts at one ordering; backward selection
starts at \code{max_intrinsic_dim}. At each step, the candidate is accepted only
if its final soft-assignment objective at \code{T = 1} is strictly larger.
For each dimension above one, both similarity and ordering-method
initializations are attempted and the higher finite objective is used.
}
\details{
This is a greedy comparison of variational solutions. The selected dimension
can depend on initialization and convergence; inspect the returned candidate
table and comparison history before interpreting it.
}
